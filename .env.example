# Copy to .env.local and fill in. All values are validated at startup
# (config/env.ts) — the app fails fast if a required var is missing/malformed.

# Required
SITE_URL=https://inchapin.ru
SITE_NAME=INCHAPIN

# Optional: lead-form delivery endpoint (CRM/email webhook)
# LEADS_WEBHOOK_URL=
